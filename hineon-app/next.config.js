// File: next.config.js

/** @type {import('next').NextConfig} */
const nextConfig = {
  // Các cấu hình khác của Next.js có thể được thêm vào đây
  // Ví dụ: reactStrictMode: true,
};

module.exports = nextConfig;